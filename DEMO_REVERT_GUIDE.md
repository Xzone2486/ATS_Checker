# Demo Revert Guide

This guide explains how to restore the original functionality of the ATS Analysis page and Authentication modal after the demo.

## 1. ATS Analysis Popup & Navigation
**Files:** `src/app/ats-analysis/page.tsx`, `src/components/layout/Navbar.tsx`, `src/components/ui/header-1.tsx`

**To Revert (Navigation Buttons):**
1. In both `Navbar.tsx` and `header-1.tsx`:
   - Search for `/* REVERT_GUIDE: To restore the login modal ... */`.
   - Revert the `onClick` handler of the "Get Started" buttons from `navigate("/ats-analysis")` to `openModal`.
   - On mobile menus, ensure `setOpen(false)` or `setMobileMenuOpen(false)` is still called before `openModal()`.

**To Revert (ATS Analysis Page Actions):**
1. In `src/app/ats-analysis/page.tsx`, search for `/* REVERT_GUIDE: Uncomment the line below ... */`.
2. Uncomment the `onClick` handler that scrolls to the mentor section.
3. Remove the line that redirects to `/improve/for_all`.

**To Revert (ATS Analysis Popup):**
1. In `src/app/ats-analysis/page.tsx`, search for the section marked `{/* REVERT_GUIDE ... */}` near the bottom of the file.
2. Uncomment the original `Button` block.
3. Remove the `{/* DEMO BUTTONS */}` block.

## 2. Authentication Modal
**File:** `src/components/auth/SignInModal.tsx`

**To Revert:**
1. Search for `{/* REVERT_GUIDE ... */}`.
2. Uncomment the **Tab switcher** block (lines ~69-81).
3. Uncomment the **Email/Password Form** block (lines ~84-142).
4. Remove the `{/* DEMO MESSAGE */}` block.
5. In the OAuth buttons section:
   - Change `grid-cols-1` back to `grid-cols-2`.
   - Restore the original button mapping (remove LinkedIn if not needed permanently).

## 3. Pricing Section
**Files:** `src/App.tsx`, `src/components/layout/Navbar.tsx`, `src/components/ui/header-1.tsx`, `src/components/layout/Footer.tsx`

**To Revert:**
1. In `src/App.tsx`, uncomment the `PricingPage` import and the `/pricing` Route.
2. In `src/components/layout/Navbar.tsx`, uncomment the `Pricing` link in the `navLinks` array.
3. In `src/components/ui/header-1.tsx`, uncomment the `Pricing` link in the `links` array.
4. In `src/components/layout/Footer.tsx`, uncomment the `Pricing` list item under the "Product" column.

## 3. General (Optional)
If you added any test data or general slugs in `mockImproveData.ts` for the demo, you can remove them or set `isActive: false`.

---
*Guide generated by Antigravity AI*
